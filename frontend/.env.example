# Base URL of the deployed backend API, no trailing slash.
# Local dev (against docker-compose backend): http://localhost:8000
# Prod: your Render service URL, e.g. https://job-tracker-backend.onrender.com
VITE_API_URL=http://localhost:8000
