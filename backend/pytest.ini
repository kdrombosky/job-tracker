[pytest]
testpaths = tests
# Without this, bare `pytest` never adds backend/ to sys.path (only
# `python -m pytest` or running scripts directly from this dir does that
# implicitly), so `from app...` fails with ModuleNotFoundError.
pythonpath = .
